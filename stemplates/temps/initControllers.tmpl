package controllers

import (
	"{{ .ProjectName }}/{{.AppName}}/repository"
	validator "github.com/go-playground/validator/v10"
)

type {{ .AppName | toPascalCase }}Controller struct {
	Svc       *repository.{{ .AppName | toPascalCase }}Repository
	Validator *validator.Validate
}

func New{{ .AppName | toPascalCase }}Controller(svc *repository.{{ .AppName | toPascalCase }}Repository ) *{{ .AppName | toPascalCase }}Controller {
	return &{{ .AppName | toPascalCase }}Controller{
		Svc:       svc,
		Validator: validator.New(),
	}
}
